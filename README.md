# parodify-ui-playwright

node
 instalação do node (codigo)
 node --version
 
desenvolvido em type script

instalação
https://playwright.dev/docs/intro

rodar a automação via web
npx playwright test --ui  

rodar no modo headless
npx playwright test 